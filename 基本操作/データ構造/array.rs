let ar = [0, 1, 2]; // [i32, 100]型

ar[1];
ar[1] = 42;

ar.len();
ar.first();
ar.last();

a.sort();
a.reverse();

a.iter() // イテレータ化？
a.into_iter() // イテレータ化？

a.swap(1, 2) // -> unit

for x in &ar {
    
}
