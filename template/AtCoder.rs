use proconio::{input, fastout};

#[allow(unused_imports)]
use std::collections::*;
#[allow(unused_imports)]
use std::cmp::*;

#[fastout]
fn main() {
    input!{
        // n: usize,
        // v: [i32; n],
    }

    let ans = 42;
    println!("{}", ans);
}
