use proconio::{input, fastout};

#[allow(unused_imports)]
use std::collections::*;
#[allow(unused_imports)]
use std::cmp::*;

#[fastout]


fn main() {
    input!{
        // n: usize,
        // v: [isize; n],
    }

    let ans = 42;
    println!("{}", ans);
}
